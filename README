This project contains a XText (http://www.xtext.org) project that defines a DSL for creating the WWF configuration file. This is part of a project to understand DSL, XText and then use DSL's for defining the configuration. At this point in time, the project has the grammar defined and some base UI configurations done.